# Copy to .env in the same directory as docker-compose.yml, then edit.
# .env is gitignored. Never commit real credentials.

# Splunk Enterprise admin password.
# Requirements: 8+ characters. Used for the `admin` account at http://<VM_IP>:8000
SPLUNK_PASSWORD=ChangeMe_StrongPassword_2026!
